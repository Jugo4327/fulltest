import { RenderMenu } from '../structure/RenderNavigation'
export const RenderHeader = () => {

    return (
        <div className= "header">
            <RenderMenu />
            <h2 className="Logo"> Jet Express</h2>

        </div>

    )

}